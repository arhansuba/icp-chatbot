"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getDfxCanisters = void 0;
const path_1 = require("path");
function getDfxCanisters(directory, dfxConfig) {
    const canisters = [];
    if (dfxConfig === null || dfxConfig === void 0 ? void 0 : dfxConfig.canisters) {
        Object.entries(dfxConfig.canisters).forEach(([alias, config]) => {
            if (!config) {
                return;
            }
            const { type, main } = config;
            if (main && (!type || type === 'motoko')) {
                canisters.push({
                    alias,
                    file: (0, path_1.resolve)(directory, main),
                    config,
                });
            }
        });
    }
    return canisters;
}
exports.getDfxCanisters = getDfxCanisters;
//# sourceMappingURL=canister.js.map