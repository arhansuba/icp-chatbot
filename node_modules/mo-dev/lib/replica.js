"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.startReplica = void 0;
const child_process_1 = require("child_process");
// Replica proxy fallback (currently unused)
function startReplica(directory, port) {
    const replica = (0, child_process_1.spawn)('dfx', [
        'start',
        // '--clean',
        '--host',
        `127.0.0.1:${port}`,
    ], {
        cwd: directory,
    });
    replica.stdout.pipe(process.stdout);
    replica.stderr.pipe(process.stderr);
    return replica;
}
exports.startReplica = startReplica;
//# sourceMappingURL=replica.js.map