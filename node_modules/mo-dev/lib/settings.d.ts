import { TestMode } from './testing';
export interface Settings {
    directory: string;
    port: number;
    delay: boolean;
    execute: string;
    verbosity: number;
    generate: boolean;
    deploy: boolean;
    deployArgs: string[];
    test: boolean;
    testModes: TestMode[];
    testFiles: string[];
    canisterNames: string[];
    reinstall: boolean;
    hotReload: boolean;
    ci: boolean;
}
export declare const defaultSettings: Settings;
export declare function validateSettings(settings: Partial<Settings>): Promise<Settings>;
//# sourceMappingURL=settings.d.ts.map