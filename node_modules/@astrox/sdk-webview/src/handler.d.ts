import { AbstractedClientStorage, BaseTransactionRequest, IWalletConnector, Wallet } from '@astrox/sdk-core';
import { DelegationChain } from '@dfinity/identity';
import { ActorSubclass, HttpAgent, PublicKey, SignIdentity } from '@dfinity/agent';
import { Principal } from '@dfinity/principal';
import { InterfaceFactory } from '@dfinity/candid/lib/cjs/idl';
import { BalanceResponseObject, SupportedToken, TransactionResponse, WebViewConnectRequest } from './types';
import './cbor';
export declare class AstroXWebViewHandler implements IWalletConnector {
    private _identity?;
    private _agent?;
    private _chain?;
    private _storage?;
    private _cacheKey?;
    private _wallet?;
    private _confirm?;
    private _host?;
    private _customDomain?;
    principal: string | undefined;
    private _isReady;
    constructor(_identity?: SignIdentity, _agent?: HttpAgent, _chain?: DelegationChain, _storage?: AbstractedClientStorage, _cacheKey?: string, _wallet?: Wallet, _confirm?: boolean, _host?: string, _customDomain?: string);
    private setCacheKey;
    private get cacheKey();
    private setWallet;
    get wallet(): Wallet;
    private setChain;
    private get chain();
    private setConfirm;
    private get confirm();
    private setIdentity;
    get identity(): SignIdentity | undefined;
    setAgent(value: HttpAgent): void;
    setHost(host: string): void;
    setCustomDomain(customDomain: string): void;
    get host(): string;
    get customDomain(): string;
    get agent(): HttpAgent | undefined;
    setStorage(storage: AbstractedClientStorage): void;
    get storage(): AbstractedClientStorage;
    private clear;
    private _injectWindow;
    private _initBridge;
    private fromStorage;
    isReady(): boolean;
    getDelegation(): DelegationChain | undefined;
    getPublicKey(): PublicKey | undefined;
    getPrincipal(): Principal | undefined;
    private _assertEnv;
    connect(params: WebViewConnectRequest): Promise<boolean>;
    reconnect(params: WebViewConnectRequest): Promise<boolean>;
    private _handleWebViewConnectResponse;
    getSupportedTokenList(): Promise<Array<SupportedToken>>;
    isConnected(): Promise<boolean>;
    disconnect(): Promise<boolean>;
    signMessage(message: any): Promise<any>;
    requestTransfer(req: BaseTransactionRequest): Promise<TransactionResponse>;
    queryBalance(): Promise<BalanceResponseObject[]>;
    config: any;
    createActor: <T>(canisterId: string, idlFactory: InterfaceFactory) => Promise<ActorSubclass<T>>;
    address(): {
        principal?: string;
        accountId?: string;
    };
    init(): Promise<any>;
}
//# sourceMappingURL=handler.d.ts.map